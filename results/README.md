# Results 경로

역할별 측정 결과의 위치를 안내한다.

- `model/`: B 소유 — Accuracy, FP32↔INT8 비교, CPU 기준 성능
- A의 Bitstream/XSA/ELF, Vivado/Vitis 로그와 Report: 로컬에서 생성·검증하되 Git에는
  커밋하지 않는다. 필요한 값과 checksum만 Handoff·integration manifest에 기록한다.

재생성 가능한 대용량 산출물을 소스 저장소에 올리지 않는다.
