# B 역할 — Model 결과

> 소유: B

Dataset/모델 버전별 Accuracy, FP32↔INT8 차이, CPU Latency, 포화율과 검증 요약을 둔다.
보고서에는 실행 환경, 명령, 입력·Weight 버전과 재현에 필요한 seed를 기록한다.
실제 Weight 파일은 `weights/`, bit-exact 출력은 `golden_outputs/`에 둔다.
